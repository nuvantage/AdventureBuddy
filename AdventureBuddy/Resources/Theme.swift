import SwiftUI

enum AdventureTheme {
    static let forest = Color("ForestGreen")
    static let trail = Color("TrailBrown")
    static let ember = Color("WarmOrange")
    static let sand = Color("SandCream")
    static let launch = Color("LaunchBackground")
}
